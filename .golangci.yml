run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - bodyclose
    - contextcheck
    - noctx

linters-settings:
  goimports:
    local-prefixes: github.com/johntdyer/unifi-kuma

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
